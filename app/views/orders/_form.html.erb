<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :total %>
    <%= form.text_field :total, id: :orders_total %>
  </div>
  <div class="field">
    <%= form.label :user_id %>
    <%= collection_select(:order, :user_id, @users, :id, :first_name) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
